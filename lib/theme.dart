import 'package:flutter/material.dart';

// ── 색상 팔레트 ──────────────────────────────
const kCardBg = Color(0xFFF5F5F7);
const kGrayLabel = Color(0xFF666666);
const kGrayTime = Color(0xFF999999);
const kErrorRed = Color(0xFFD32F2F);
const kUpColor = Color(0xFFD32F2F);
const kDownColor = Color(0xFF1976D2);
const kAccentBlue = Color(0xFF3366FF);

// ── 대시보드 카드 그리드 설정 ──────────────────────────────
const double kMinCardWidth = 140;
const double kCardGap = 12;
const double kScreenPadding = 16;
